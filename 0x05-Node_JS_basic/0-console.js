/**
 * displayMessage - prints a message to the standard output
 * @message: the message to print
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;

